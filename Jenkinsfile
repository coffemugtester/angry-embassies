pipeline {
    agent any

    stages {
        stage('Say Hello') {
            steps {
                echo 'Hello world!'
                // Optionally, you can print an additional message to confirm execution
                echo 'This is the Say Hello stage.'
            }
        }
    }
}
